add a feature to 
change the world "up" direction:

for example :

camera.up.set(0, 0, 1);  // Make Z the up axis
controls = new OrbitControls(camera, renderer.domElement);


provide a button to loop through all possibilities 